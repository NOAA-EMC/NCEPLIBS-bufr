	MODULE MODA_MSGCWD

#ifndef NFILES_H
#define NFILES_H
	  USE MODV_NFILES
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, ALLOCATABLE :: NMSG(:)
	  INTEGER, ALLOCATABLE :: NSUB(:)
	  INTEGER, ALLOCATABLE :: MSUB(:)
	  INTEGER, ALLOCATABLE :: INODE(:)
	  INTEGER, ALLOCATABLE :: IDATE(:)
#else
	  INTEGER :: NMSG(NFILES)
	  INTEGER :: NSUB(NFILES)
	  INTEGER :: MSUB(NFILES)
	  INTEGER :: INODE(NFILES)
	  INTEGER :: IDATE(NFILES)
#endif

	END MODULE
