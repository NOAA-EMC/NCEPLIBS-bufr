	MODULE MODA_NMIKRP

#ifndef MAXCD_H
#define MAXCD_H
	  USE MODV_MAXCD
#endif

#ifdef DYNAMIC_ALLOCATION
	  CHARACTER*8, ALLOCATABLE :: NEM(:,:)
	  INTEGER, ALLOCATABLE :: IRP(:,:)
	  INTEGER, ALLOCATABLE :: KRP(:,:)
#else
	  CHARACTER*8 :: NEM(MAXCD,10)
	  INTEGER :: IRP(MAXCD,10)
	  INTEGER :: KRP(MAXCD,10)
#endif

	END MODULE
