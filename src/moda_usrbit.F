	MODULE MODA_USRBIT

#ifndef MAXSS_H
#define MAXSS_H
	  USE MODV_MAXSS
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, ALLOCATABLE :: NBIT(:)
	  INTEGER, ALLOCATABLE :: MBIT(:)
#else
	  INTEGER :: NBIT(MAXSS)
	  INTEGER :: MBIT(MAXSS)
#endif

	END MODULE
