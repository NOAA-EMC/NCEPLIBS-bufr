	MODULE MODA_MSGLIM

#ifndef NFILES_H
#define NFILES_H
	  USE MODV_NFILES
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, ALLOCATABLE :: MSGLIM(:)
#else
	  INTEGER :: MSGLIM(NFILES)
#endif

	END MODULE
