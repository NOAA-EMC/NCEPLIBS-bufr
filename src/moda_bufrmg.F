	MODULE MODA_BUFRMG

#ifndef MXMSGL_H
#define MXMSGL_H
	  USE MODV_MXMSGL
#endif

#ifndef NFILES_H
#define NFILES_H
	  USE MODV_NFILES
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, ALLOCATABLE :: MSGLEN(:)
	  INTEGER, ALLOCATABLE :: MSGTXT(:,:)
#else
	  INTEGER :: MSGLEN(NFILES)
	  INTEGER :: MSGTXT(MXMSGLD4,NFILES)
#endif

	END MODULE
