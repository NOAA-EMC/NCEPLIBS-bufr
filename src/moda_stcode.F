	MODULE MODA_STCODE

#ifndef NFILES_H
#define NFILES_H
	  USE MODV_NFILES
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, ALLOCATABLE :: ISCODES(:)
#else
	  INTEGER :: ISCODES(NFILES)
#endif

	END MODULE
