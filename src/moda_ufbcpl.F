	MODULE MODA_UFBCPL

#ifndef NFILES_H
#define NFILES_H
	  USE MODV_NFILES
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, ALLOCATABLE :: LUNCPY(:)
#else
	  INTEGER :: LUNCPY(NFILES)
#endif

	END MODULE
