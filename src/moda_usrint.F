	MODULE MODA_USRINT

#ifndef MAXSS_H
#define MAXSS_H
	  USE MODV_MAXSS
#endif

#ifndef NFILES_H
#define NFILES_H
	  USE MODV_NFILES
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, ALLOCATABLE :: NVAL(:)
	  INTEGER, ALLOCATABLE :: INV(:,:)
	  REAL*8, ALLOCATABLE :: VAL(:,:)
#else
	  INTEGER :: NVAL(NFILES)
	  INTEGER :: INV(MAXSS,NFILES)
	  REAL*8 :: VAL(MAXSS,NFILES)
#endif

	END MODULE
